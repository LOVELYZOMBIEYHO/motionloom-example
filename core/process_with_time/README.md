# Core Process With Time Examples

Timed process examples live here so `core/process` can stay static and editor-friendly.

Use these examples when the process effect itself demonstrates `curve(...)` or other time-based parameter animation.
