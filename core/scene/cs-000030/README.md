# Scene Layer3D Cards

Demonstrates AE-like 2.5D scene layers. `Layer3D` keeps regular 2D scene
content, renders the layer to a texture, then projects its four corners through
a perspective camera plane for card-like X/Y rotation.
