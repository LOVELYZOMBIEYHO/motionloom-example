# Edge Softness

Applies `<EdgeSoftness />` to a composited Group. Only alpha boundaries are softened, while opaque interior color remains sharp.

Use `radius` for edge width, `amount` for the mix, and `preserveInterior` to protect solid regions.
