# Universal Procedural Material

Defines one reusable `<Noise />` and `<Material />` resource. The same material drives GPU procedural texture, displacement, roughness, and specular shading on a `<Group>` and a `<Path>`.

`material="..."` is also supported by `<Svg />` and `<Image />` nodes.
