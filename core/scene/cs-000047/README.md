# CS-000047 Puppet Warp Bound Pins

Demonstrates target-based `PuppetWarp` and semantic `PuppetPin bindTo` anchors.
The pin target coordinates are animated while the original character artwork
remains a normal editable Scene Group.
