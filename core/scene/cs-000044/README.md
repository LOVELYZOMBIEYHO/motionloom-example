# Edge Roughness

Uses `<EdgeRoughness />` to displace only the alpha boundary with deterministic procedural noise.

`radius` controls maximum displacement, `amount` controls strength, `scale` controls variation size, and `seed` creates repeatable styles.
