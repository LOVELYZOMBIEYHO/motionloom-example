# Color Bleed

Uses `<ColorBleed />` to extend nearby opaque pigment into transparent edge pixels, resembling ink, watercolor, or cel-paint spread.

`radius` chooses the donor-pixel distance and `amount` controls how much color and alpha leave the original boundary.
