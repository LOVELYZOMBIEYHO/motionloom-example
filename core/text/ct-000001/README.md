# Text Pill Box

ID: `ct-000001`  
Type: `core`  
Domain: `text`

## Features

text, text-box, text-gap

## DSL

Graph, Background, Scene, Timeline, Track, Sequence, Layer, Text, fontWeight, textGap, box

## Teaches

- Use `box="pill"` to create an auto-sized rounded background behind text.
- Use `fontWeight` and `textGap` to tune bold condensed title-card typography.
- Use transparent `Background` for overlay-ready pill text exports.

## Use In Anica

Open `main.motionloom`, copy all content, then paste it into the Anica VFX / MotionLoom page.
