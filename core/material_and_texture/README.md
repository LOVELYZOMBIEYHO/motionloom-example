# Core Material and Texture Examples

Standalone MotionLoom examples for material looks and procedural texture studies.

These examples are intentionally kept outside the generated index for now because
`cm-*` is currently used by the existing `composition` domain index.
