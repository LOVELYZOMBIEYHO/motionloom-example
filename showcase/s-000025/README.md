# s-000025 Base64 Oil Canvas Texture Parameters

Self-contained showcase based on `cm-000007`. The oil-canvas texture is embedded as base64 and the texture overlay parameters change every two seconds.
