# s-000026 Base64 Paper Magnifier Typography

A six-second paper texture showcase with a moving `magnify_lens` process pass over bold typography. The paper JPEG is embedded directly as a base64 data URI, then `texture_overlay` adds tactile printed-paper material after the lens remap.
