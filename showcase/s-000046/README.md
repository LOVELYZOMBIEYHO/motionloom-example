# AnimationTarget Kinetic Shapes

This showcase keeps the art simple, then uses dense `AnimationTarget` keyframes to create complex motion.

It demonstrates:

- Targeting scene nodes by `id`.
- Animating `x`, `y`, `rotation`, `scale`, and `opacity`.
- Keeping visual primitives easy to inspect while making the animation editable by UI tools.
